# 使用 Codex 导出微信聊天记录｜同事分享版

整理日期：2026-09-21（Codex 开源版 v3）  
适用工具：Windows 上的 Codex Desktop。  
脚本依据：项目内 2026-09-02 更新的客户筛选版。

这套方法可以把 Windows 微信本机已有的、本人确认要导出的客户私聊，整理成 **Excel 索引 + HTML 网页 + CSV 数据 + 交付 ZIP**。流程是：环境检查 → 确认账号 → 本机快照与解密 → 本人勾选会话 → 导出 → 对账打包。

**请把本说明与 `微信聊天记录一键导出-Codex客户筛选版.zip` 一起发给同事。MD 是说明书，ZIP 是配套程序。**

## 0. 先克隆到自己的电脑，聊天数据不上 GitHub

在自己的 Windows PowerShell 中执行：

```powershell
git clone https://github.com/disuxiaoshuo339-beep/wechat-chat-export-codex.git
cd wechat-chat-export-codex
```

然后用 Codex Desktop 打开这个本地项目，按根目录 README 和 AGENTS.md 操作。更新工具代码可执行 `git pull --ff-only`。普通使用无需 fork、提交或 push；不要在 GitHub Actions/Codespaces 中导出。

实际联系人、会话清单、聊天正文、数据库、密钥、日志及导出 ZIP 只留在本机，不上传提交、Issue、PR、Gist、Release 或附件，也不粘贴到 AI 对话。默认输出为 `%LOCALAPPDATA%\WeChatChatExport\runs`，位于工具目录之外。不要把聊天内容粘进已有 README 或源码文件。

## 1. 本次使用的版本

本说明专门按 **Codex Desktop** 的操作方式整理。配套包基于项目中 2026-09-02 的客户筛选版，保留「本人勾选会话 → 只导出勾选项 → 核验后清理中间数据库」流程。

开源版增加了仓库外默认输出、运行路径检查、索引公式转义和 Git 防误提交规则，不需要安装全局 Skill。“最新”指本次在项目相关目录中找到的较新流程，不代表互联网上工具的最新发布。

## 2. 能导出什么

- **范围**：已授权账号、本机数据库中能识别的一对一会话，且经过本人勾选确认。
- **默认排除**：群聊、公众号、系统账号及未勾选会话；企业微信联系人 `@openim` 的私聊可以进入筛选清单。
- **结果**：每个选中会话一份 HTML 和一份 CSV，另有客户索引、数量摘要及说明。
- **记录内容**：时间、发送方向、消息类型和可解析的文本／卡片内容等。
- **多媒体边界**：当前程序整理数据库中的消息记录；它不是原图、语音音频、视频和附件的完整备份工具，也不提供语音转写。非文本消息可能呈现类型提示或原始描述。
- **历史边界**：只能处理本机已有数据，不能据此恢复已删除记录，也不能保证手机独有、尚未同步到电脑的历史包含在内。

## 3. 开始前准备

1. 使用 Windows 电脑，登录要处理的微信账号，并在全过程保持微信运行。工具目标为微信 4.x，具体小版本仍需本机检查与数据库校验。
2. 确认自己有权处理该账号及相关记录。
3. 安装 Python 3.11 或以上；在终端运行 `python --version` 能看到版本号。
4. 推荐按第 0 节克隆仓库；也可解压工具 ZIP 到自己的工作目录。克隆用户打开仓库根目录；ZIP 用户找到同时包含 `START_HERE.md`、`AGENTS.md` 和 `wechat-local-chat-export` 文件夹的那一层。
5. 打开并登录 Codex Desktop，将上一步找到的文件夹添加为**本地项目的主文件夹**，然后在该项目里新建任务。确认任务在这台 Windows 电脑执行，能访问解压目录和本机微信数据。
6. 输出位置放在微信数据目录之外，并为数据库快照、解密副本和导出结果预留空间。

Codex 本地项目的主文件夹用于新任务的默认工作目录和 `AGENTS.md` 等项目文件的自动发现；克隆用户选择仓库根目录，ZIP 用户选择含 `START_HERE.md` 的那一层。参见 [OpenAI 官方文档：本地项目与文件夹](https://learn.chatgpt.com/docs/projects#use-local-projects-for-folders-and-codebases)。

依赖由预检确认：`pycryptodomex` 用于数据库解密；`zstandard` 用于解析压缩内容，Python 3.14 的内置 zstd 支持可替代它；`openpyxl` 用于生成 Excel，缺少时可以降级为 CSV 索引。缺依赖时先了解用途，再同意安装。

## 4. 普通同事照着操作

### 第一步：复制启动指令

在上述本地项目的 Codex 任务中，复制发送：

```text
请确认当前任务使用这台 Windows 电脑上的解压目录。
先阅读当前目录的 AGENTS.md，按它指向的 START_HERE.md 和 SKILL.md 操作，
按客户筛选版流程帮我导出微信聊天记录。现在只开始只读预检。

预检后列出准确账号 ID，等我明确授权；生成会话清单后等我本人勾选。
不要代我选择账号，不要打开会话清单查看联系人，不要在对话里展示聊天正文。
聊天数据只在本机处理，不上传数据库、密钥、会话清单或聊天内容。
使用仓库外的默认本地输出位置；不执行 git add/commit/push。
```

这一步只检查环境和发现账号，不开始实际导出。

### 第二步：明确选择账号

Codex 列出账号后，核对并回复：

```text
授权处理账号：<把这里替换为预检列出的完整 account_id>。
按客户筛选版操作，先生成会话清单，等我确认后再导出。
```

即使只有一个账号，也要明确确认。多个账号一次处理一个，不根据目录大小或最近使用时间猜测。

### 第三步：本人核对会话清单

快照与解密完成后，Codex 会给出 `会话清单.csv` 的路径。**由本人在 Excel 等本地工具中打开，不把清单发给 AI 代看。** 清单包含备注、昵称、微信号、消息数和时间范围，不包含聊天正文。

只修改最后一列「是否客户」，保留表头和「微信号」列：

| 标记 | 当前含义 | 应如何处理 |
|---|---|---|
| `Y` | 会进入导出范围；可能是脚本预填的 | 每一项都要核对；不需要导出的清空 |
| 空白 | 不导出 | 需要导出的改成 `Y` |
| `?` | 待人工判断 | 判断后改成 `Y` 或清空 |

自动判定主要根据涂料／化工／公司等关键词、备注形态及手机号等线索，**可能误判；预填 `Y` 不等于已经得到本人确认**。非涂料行业同样需要逐项复核，不能依赖这些行业关键词。

保存为 **CSV UTF-8（逗号分隔）**，确认仍为 `.csv`，关闭文件后回复：

```text
会话清单已逐项核对并保存。只导出“是否客户”为 Y 的会话。
请生成索引、核对数量并打包；ZIP 校验通过后，
使用 --purge-workspace 清理本次运行的 source_copy 和 private 中间目录。
```

### 第四步：领取结果

等待 Codex 给出 `complete` 状态、交付 ZIP 路径、已导出会话数、消息数和筛除会话数。解压 ZIP 后，先打开客户索引，再查看所需 HTML 或 CSV。

正常交付 ZIP 的结构如下：

```text
微信客户聊天记录_交付.zip
├─ 客户聊天索引.xlsx       # 无 openpyxl 时为客户聊天索引.csv，二选一
├─ 聊天记录_HTML/
│  └─ 每个选中会话一份网页
├─ 聊天记录_CSV/
│  └─ 每个选中会话一份数据文件
├─ 导出摘要.json
└─ README.txt
```

## 5. 怎样确认导出成功

不能只看到 ZIP 就认定成功，至少核对：

- 人工选中会话数 = 实际导出会话数；`selection_not_found` 应为 `0`。如不一致，先排查清单中的账号标识是否被误改。
- 索引中的会话行数 = HTML 文件数 = 会话 CSV 文件数。
- 每个会话的消息数与对应 CSV 的数据行数一致；导出消息总数 = 我发送 + 客户发送 + 未知方向。
- 已导出会话数 + 已筛除会话数 = 此次扫描识别的一对一会话总数。
- ZIP 通过文件清单检查及 CRC 完整性校验；不包含数据库、密钥、日志、会话清单和源快照。
- 清理结果应包含本次运行的 `source_copy` 和 `private`；若清理失败，处理完成前不要把整个运行目录分享出去。

脚本会做部分自动对账，但 `complete` 不证明手机和电脑的历史记录完全一致，也不能代替本人对导出范围的确认。

## 6. 数据处理边界

**这是本地脚本处理流程，不等于 Codex 完全离线。** Codex 使用过程中及安装依赖时可能联网；执行过程中应只向对话返回必要的阶段、状态、数量和路径，不读取或粘贴联系人清单与聊天正文。

人工筛选控制的是后续正文查询和最终导出范围。筛选之前，程序会读取本机微信进程及数据库副本进行解密；`private` 下的解密数据库可能包含未勾选的私人会话。因此不能把这套流程表述为“私人数据完全不被处理”，也不能把提示词限制当作文件访问隔离。

`--purge-workspace` 在 ZIP 校验通过后删除本次运行的 `source_copy`（源数据库快照）和 `private`（含解密数据）。它**不会自动删除运行目录中的 `会话清单.csv`**；该文件仍含联系人元数据，需要本人妥善保管或在不再需要时自行删除。中途失败也不能假定中间文件已清理。

分享操作方法时发送公开仓库链接即可。实际聊天交付 ZIP 含个人／业务数据，只留在本人电脑；不要发到 GitHub，也不要发送整个运行目录。

## 7. 常见问题

| 问题 | 处理方式 |
|---|---|
| 找不到账号 | 确认微信已在本机登录运行；检查实际数据路径。技术同事可用 `preflight.py --root "数据搜索路径"` 指定搜索位置，后续快照也要使用相同搜索根目录 |
| 微信版本不兼容、无法读取进程或密钥覆盖不全 | 停止本次导出，保留错误类型交技术人员排查；不要跳过校验，也不要把部分结果当成完整结果 |
| 清单一个 `Y` 都没有 | 本人重新确认导出范围；不需要导出任何会话时直接结束，不必为了跑通而勾选 |
| 中文乱码或保存成了 XLSX | 重新保存为 CSV UTF-8，保留原始表头及微信号列 |
| 没有 Excel 索引 | 检查是否已经生成 CSV 索引；`openpyxl` 可选，CSV 同样可用。需要改成 XLSX 时在新的运行中处理，避免混留两个索引版本 |
| 提示交付目录已存在 | 保留历史结果，新建一次运行；不要直接覆盖或删除旧交付 |
| 想重新勾选后再导出 | 优先创建新的运行目录并重新筛选；已经清理中间数据库的运行必须重新快照和解密 |
| 图片、语音、视频没有原文件 | 属于当前方法的能力边界，不代表能靠重新打包补齐 |
| 只想导出群聊或完整多媒体 | 本附件包当前不覆盖该目标，需要另行确认方案 |

## 8. 技术同事用：实际脚本顺序

以下命令在解压后的 `wechat-local-chat-export` 目录运行；把尖括号占位符替换为本机真实值。**分阶段执行，不要把全部命令一次性运行。**

只读预检：

```powershell
python scripts/preflight.py
```

取得本人对准确账号的明确授权后，建立快照并解密。默认输出为 `%LOCALAPPDATA%\WeChatChatExport\runs`；自定义路径也必须在 Git 仓库、工具目录及微信源账号目录之外：

```powershell
python scripts/prepare_snapshot.py --account-id "<准确账号ID>"
```

记下返回的 `run_root`，后续始终使用同一次运行的路径：

```powershell
python scripts/list_conversations.py --run-root "<本次run_root绝对路径>"
```

**到这里停下，由本人打开会话清单并确认保存。得到「已确认」回复后再执行：**

```powershell
python scripts/run_export.py --run-root "<本次run_root绝对路径>"
python scripts/build_index.py --run-root "<本次run_root绝对路径>"
```

核对选中数与实际导出数、`selection_not_found` 等结果。异常先处理，不继续打包。最后运行：

```powershell
python scripts/finalize_delivery.py --run-root "<本次run_root绝对路径>" --purge-workspace
```

`--purge-workspace` 会删除本次运行的两个中间目录，应在上述清理范围已由本人确认后使用；不得把运行路径指向微信源数据或其他历史目录。

## 9. 资料依据与本次核验

导出流程依据项目客户筛选版的 `START_HERE.md`、`AGENTS.md`、`wechat-local-chat-export/SKILL.md`、两份 `references` 文档及实际脚本；Codex 本地项目步骤依据上文链接的 OpenAI 官方文档。

开源版新增运行路径检查及索引公式转义；`.gitignore` 仅允许明确列出的源码、说明及工具包，新文件默认忽略。发布检查、测试结果及实际工具 ZIP 文件数以仓库 `PRIVACY_CHECK.md` 为准。测试中的联系人、号码、账号及消息均为合成样例。

本次没有启动微信导出，没有读取真实会话清单或聊天正文，没有验证同事电脑上的具体微信小版本。首次使用仍须由 Codex 做本机预检，并完成账号确认、会话筛选和导出对账。

同事收到的两个文件应为：

```text
微信聊天记录导出方法_Codex版.md
微信聊天记录一键导出-Codex客户筛选版.zip
```
