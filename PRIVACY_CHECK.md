# 开源发布前检查

检查日期：2026-09-21。范围：本次发布的源码、文档、工具 ZIP，以及转公开前的全部既有 Git 历史和历史工具 ZIP。

## 结论

本次检查未发现真实微信账号、真实联系人资料、真实聊天正文、个人邮箱、凭据或真实解密密钥。测试中的短账号、姓名、手机号和消息均为代码构造的合成样例。代码中的固定解码常量用于协议解析，不是某位用户的数据库密钥。

公开仓库包含 52 个文件，工具 ZIP 内含 44 个文件，仅为源码、说明、MIT 许可证和忽略规则。没有数据库、运行日志、进程转储或实际导出记录。

## 已处理的具体风险

1. 旧流程在没有 D 盘时会回退到项目内的 outputs。现在默认输出为 `%LOCALAPPDATA%\WeChatChatExport\runs`，标准入口拒绝把运行数据写入 Git 仓库或工具目录；建立快照前也检查与微信源账号目录的重叠。
2. Git 忽略规则改为明确文件白名单，未列出的新文件默认忽略，包括 Markdown、JSON、CSV、数据库及导出 ZIP。根目录和附件包的 Codex 指令均禁止普通使用任务执行 git add/commit/push 或上传运行数据。
3. 索引表中的外部备注／昵称按文本转义，防止以等号等字符开头的内容成为 Excel 公式；CSV 降级索引同步处理。
4. 已删除旧机器安装路径的注释。发布文件没有原维护者的用户目录或私人邮箱。Git 提交只使用公开 GitHub 账号的 noreply 邮箱。

## 验证记录

- 完整合成测试：75 项通过，1 个目录符号链接用例因本机环境限制跳过。测试不读取真实微信数据库，不扫描真实微信进程。
- 16 个模拟私人产物路径通过 Git 忽略验证；包括用户临时新建的 .md 和 .py 文件。
- 发布前原有 1 个提交、44 个去重 Git blob 和历史工具 ZIP 均已检查；新提交和重建 ZIP 在发布前再次扫描。
- 标准运行脚本未发现网络客户端导入或上传实现；对源码、说明及压缩包内容扫描账号、电话、私人路径和常见凭据标记，并检查命中项。
- 工具 ZIP 文件清单、逐文件内容及 CRC 校验通过；仓库文件校验值见 SHA256SUMS.txt。
- 原私有仓库没有 GitHub Actions 运行记录、Release、Issue 或 PR，也没有待处理的协作者邀请。

## 实际使用的边界

这些检查不表示绝对零风险，也不替代具体微信版本的实机兼容检查。此次没有执行真实账号导出。

筛选前的本机解密中间库可能含未选会话；`--purge-workspace` 只删除本次运行的源快照和私有中间目录，不是安全擦除，也不自动删除会话清单。中途失败可能保留中间文件。

Codex 本身可能联网，用户应只让脚本处理本地数据，不把联系人或聊天正文送入对话。Git 忽略规则无法阻止人为 git add -f，或把私人内容写进已经受版本控制的源码／说明；因此这些操作也在使用说明中明确禁止。

将仓库设为公开会公开工具源码、文档和 GitHub 账号的提交记录；不会因此读取使用者电脑或自动上传他未来产生的聊天数据。用户必须先克隆到自己的电脑，再按本地流程使用。
