# Codex 本地使用规则

本仓库是公开的工具源码。普通使用必须先 clone/pull 到用户自己的 Windows 电脑，再在本机运行。不要把导出任务放到 GitHub Actions、Codespaces 或远程云端。

## 开始前

阅读 `微信聊天记录一键导出-Codex客户筛选版/START_HERE.md`，再阅读其中指向的 SKILL 和 references。脚本位于该附件包内的 `wechat-local-chat-export/scripts`。

开始请求仅授权只读预检。准确 account_id 由用户明确确认；生成会话清单后，由用户本人在本地打开并勾选，收到确认再导出。不得代看清单或在对话里展示真实联系人与正文。

## 数据边界

- 使用 `%LOCALAPPDATA%\WeChatChatExport\runs` 默认输出，或用户选择的仓库和工具目录之外的本机目录；不要选择同步到云端的目录。
- 真实聊天正文、联系人、会话清单、数据库、密钥、进程数据、日志、导出 CSV/HTML/Excel/ZIP 全部是本地私人数据，不上传 GitHub，不写入已有受版本控制的文件，也不粘贴到模型上下文。
- 普通导出任务不执行 git add/commit/push，不使用 git add -f，不创建携带运行数据的 Issue、PR、Gist、Release 或附件。
- .gitignore 是防误提交措施，无法防止人为强制添加或修改已有受控文件，不得声称绝对防泄露。
- 清理只作用于已确认的本次运行目录；不删除源微信数据。中途失败需说明本机中间数据可能仍在。

## 源码维护例外

用户明确要求维护或发布工具源码时，可以提交经过检查的代码、文档及仅含工具的 ZIP。发布前检查暂存区、历史和 ZIP 成员，确认没有真实运行数据；新源码文件须明确加入 .gitignore 白名单。这个授权不能用于上传聊天数据。测试只用合成数据，不启动真实微信扫描或导出。
